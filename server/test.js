var expect = require('chai').expect;
var app = require('./client/newApp.js');

describe('Testing Graph Functionality', function(){
  var graph = new app.Graph();

  it('should add nodes properly', () => {
    g.addNode();
  });
})
